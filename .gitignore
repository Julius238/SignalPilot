.env
.env.*
# Vorlagen ohne Secrets gehören ins Repo — die echten .env-Dateien nie.
!.env.example
!.env.production.example
!.env.local.example

# Production backups — keep the directory, not the dumps
backups/*.dump
backups/*.sql
backups/*.gz

node_modules/
dist/
.next/
*.tsbuildinfo

coverage/
.DS_Store

packages/database/generated/
packages/database/prisma/generated/
**/generated/prisma/
**/.prisma/
.claude/launch.json
